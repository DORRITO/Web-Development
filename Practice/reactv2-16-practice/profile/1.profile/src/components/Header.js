import React from 'react'
import { Button, Form, Grid, Header, Image, Message, Segment } from 'semantic-ui-react'

const AppHeader = () => (
  <Header inverted color={'green'}>Yesss</Header>
)

export default AppHeader;