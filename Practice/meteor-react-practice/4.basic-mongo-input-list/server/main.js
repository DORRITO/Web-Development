import { Meteor } from 'meteor/meteor';
import {Names} from './../imports/api/names';

Meteor.startup(() => {
  // Names.insert({
  //     name: 'CHIp',
  //   });
  // console.log(Names.find().fetch())
});
